# Tech Audit Memory

- Last run: 2026-08-28 11:15 UTC
- Open issues: 94 total (3 P1, 90 P2) + 1 INFO
- Last clean run: never

## Standing context
- Step A.2.4 Performance migration ran 2026-08-09 and is complete — do NOT run it again. Performance findings use stable issue text (`LCP above threshold`, `CLS above threshold`, `INP above threshold`, `Performance score below target`) with no metric values. Measured numbers live in `performance-report.md` only.
- **2026-08-28: PageSpeed lab-run flakiness recurred, worse than the 2026-08-17 instance — always verify a sweep before trusting it.** The first full sweep of 50 URLs came back with 49/50 URLs failing and several pages (previously fixed in commit `eaae14f`/PR #70) showing LCP back at 8-12s — `/contact` 12095ms, `/roofs-for-heroes` 10006ms, `/book-appointment` 7970ms, `/services/roof-installation` 5576ms. Checked `lcp-breakdown-insight` subpart durations on 3 of those: they summed to only ~10-15% of the reported LCP (e.g. `/contact` breakdown summed to ~1496ms vs a reported 12095ms) — same signature as the 2026-08-17 flakiness note, just larger. Retested those 6 URLs individually and got clean results matching the known-good post-fix baseline (`/contact` 2176ms, `/roofs-for-heroes` 2026ms, `/services/roof-installation` 2026ms). Concluded the entire first sweep was bad (likely PSI infra congestion during that run, not per-page noise) and reran the full 50-URL sweep from scratch rather than patching individual entries. Second sweep's breakdown-insight sums were ~55-60% of reported LCP on spot-checked P1s — still not 1:1 (some inherent slack between the discrete subparts table and total LCP is normal) but nowhere near the 8-10x mismatch that flagged the first sweep as bad. Used the second sweep as this run's data.
- **Actionable takeaway for future runs:** if a sweep comes back with most of the inventory failing, or if pages known to be fixed show poor scores again, do NOT write findings from it — spot-check 2-3 outliers' `lcp-breakdown-insight` subpart sum against the reported LCP first. A large sample of pages simultaneously regressing is a stronger tell of infra flakiness than of a real sitewide incident (a real incident is usually caused by one shared change — a deploy, a CDN issue — and CDN/deploy status should be checked before assuming that, but a lab-measurement artifact is far more common). When in doubt, discard and rerun the full sweep rather than cherry-picking retests into a mixed dataset.
- This run's Performance churn (24 new / 19 closed against 89 open) is mostly borderline pages flipping across the 2500ms/4000ms LCP threshold between runs — normal lab variance for pages sitting near the line, not a stable-identity regression (issue text stayed metric-free; matched on Module+URL+Issue as designed).
- Two new blog documents appeared in Sanity since the 2026-08-17 run: `what-to-expect-roof-rejuvenation-frederick-md` (added to sitemap.xml, `featuredImage` missing — H-051) and `replacing-gutters-with-new-roof-frederick-md` (live at 200, internally linked from `/blog` and the author page, but **not** in sitemap.xml — H-049 — and also missing `featuredImage` — H-050).
- Health and Technical SEO/GSC remain otherwise structurally clean: 50/50 URLs return 200 with 0 redirect hops (sitemap grew from 49→50 URLs this run), no dead internal links (aside from the not-in-sitemap live page above), no orphans, no missing/duplicate meta tags, no canonical or JSON-LD defects, all 50 `<lastmod>` present. The only non-Performance findings are the same 10 `servicesImage` gaps (H-017 to H-025, H-047, unchanged since 2026-06-24/2026-08-09) plus 3 `featuredImage` gaps (H-048 recurring, H-050/H-051 new) and 1 INFO noindex flag on `/blog/how-much-do-new-gutters-cost-maryland` (S-079, unchanged).
- Sitemap lives at `https://shumakerroofing.com/sitemap.xml` (non-www). The www host 308-redirects to non-www, so fetch with `curl -L`; the `<loc>` values are all non-www with no trailing slash.
- PageSpeed Insights sweep of 50 URLs takes ~8-9 minutes sequentially (1s delay/call + ~7-10s API latency each) — run as a background Bash job with a Monitor watching for a `DONE` sentinel line rather than a single foreground call (10-minute tool timeout can cut it off partway). Budget for the possibility of needing a full second sweep if the first looks systemically bad (see flakiness note above) — that's ~18 minutes of PSI time total in the worst case.
- Machine clock is Asia/Manila (UTC+8); local date runs a day ahead of UTC. Report timestamps use UTC.
