# Tech Audit Memory

- Last run: 2026-09-11 07:50 UTC
- Open issues: 85 total (11 P1, 74 P2)
- Last clean run: never

## Standing context
- Step A.2.4 Performance migration ran 2026-08-09 and is complete — do NOT run it again. Performance findings use stable issue text (`LCP above threshold`, `CLS above threshold`, `INP above threshold`, `Performance score below target`) with no metric values. Measured numbers live in `performance-report.md` only.
- **Recurring gotcha — Node `/tmp/...` path resolution on Windows:** a Bash `curl -o /tmp/x.json` writes correctly (bash translates `/tmp` to the real Windows temp dir), but a `node -e` one-liner invoked from that same Bash session resolves a literal `/tmp/x.json` argument relative to the project's `D:\` drive (`D:\tmp\x.json`) and throws `ENOENT` — silently, since the bash loop had no `set -e`. This broke the 2026-09-11 run's first PSI sweep: all 50 successful (200) API responses failed to parse and were lost, while only the 2 genuine `http_500` errors got logged, giving a false impression of an almost-empty dataset. **Fix used:** always pass the full `C:/Users/franc/AppData/Local/Temp/...` path to any `fs.readFileSync`/`fs.appendFileSync` call inside a `node -e` script — never a bash-style `/tmp/...` path — even though the same path works fine for `curl -o`, `cat`, `grep`, etc. within pure-bash commands.
- **2026-09-11: PSI lab flakiness recurred, 3rd run in a row.** Sweep 1 (50/52 failing, bimodal LCP) matched the known infra-congestion signature. Sweeps 1-2 disagreed on 25/52 URLs. Ran a 3rd sweep; used lowest-LCP-of-3 per URL as the working dataset. 9 URLs were consistently high (8000-19000ms) across all 3 independent sweeps with zero low readings — treated as confirmed real regressions rather than flaky/inconclusive, since 3-for-3 agreement is much stronger evidence than the "never got a clean reading" cases from 2026-09-09 (which were still logged as inconclusive because they lacked this level of cross-sweep confirmation). The 9: `/blog`, `/contact`, `/terms-and-conditions`, `/services/residential-roofing`, `/blog/what-to-expect-roof-rejuvenation-frederick-md`, `/blog/how-much-do-new-gutters-cost-maryland`, `/blog/how-long-do-gutters-last`, `/blog/when-to-get-roof-rejuvenation-frederick-md`, `/blog/how-marylands-summer-heat-affects-roofs-in-frederick-md`. `/contact` in particular has now shown severe LCP (11.7s-19s) across three separate 2026-09 runs — this is very likely a genuine, persistent regression worth investigating directly rather than waiting on more audit cycles.
- **Actionable takeaway carried forward:** budget for 3+ PSI sweeps per run when the first sweep shows >80% of URLs failing (the discard-signature). A 3rd sweep, used to find per-URL cross-sweep agreement (not just "did it improve"), is what separates genuine regressions from lab noise — single-sweep or even 2-sweep-disagreement data should not be reported as confirmed.
- This run resolved: H-018/H-020 (`servicesImage` now present for `gutter-installation`/`roof-rejuvenation`), H-051 (`featuredImage` now present for `what-to-expect-roof-rejuvenation-frederick-md`), S-081/S-082 (meta descriptions now present on `replacing-gutters-with-new-roof-frederick-md` and `hagerstown-md`), S-083/S-084 (og:description now present, same two URLs), S-079 (noindex removed from `how-much-do-new-gutters-cost-maryland`). New: none in Health/SEO. 4 new Performance findings (previously-closed metric/URL combos recurred with fresh IDs per the "reopen closed, else new ID" rule — see findings.md).
- Sitemap unchanged at 52 URLs since 2026-09-09. Health and Technical SEO otherwise clean: 52/52 URLs return 200 with 0 redirect hops, no dead internal links, no orphans, no duplicate titles, no canonical defects, all JSON-LD blocks valid (LocalBusiness/RoofingContractor, Article, FAQPage checked via Node JSON parser, `@graph`-nested types handled).
- Sitemap lives at `https://shumakerroofing.com/sitemap.xml` (non-www). The www host 308-redirects to non-www — fetch with `curl -L`; `<loc>` values are all non-www with no trailing slash.
- GSC readiness fully clean: verification tag present, GTM-MHJVRR9 detected, sitemap reachable and matched in robots.txt, canonical domain enforced via 1-hop 308 redirect.
- Machine clock is Asia/Manila (UTC+8); local date runs a day ahead of UTC. Report timestamps use UTC.
- **Tooling note:** this session's Bash environment has `jq` and `node` available but no `python3` — use those for JSON/HTML parsing instead. Windows path translation: bash `/tmp/foo` corresponds to `C:\Users\<user>\AppData\Local\Temp\foo` for the Read/Write tools (use `cygpath -w` to convert). See the Node `/tmp` gotcha entry above — it applies specifically to `node -e` scripts invoked from Bash, not to Bash itself.
