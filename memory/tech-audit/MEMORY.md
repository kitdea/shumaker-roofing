# Tech Audit Memory

- Last run: 2026-08-17 06:35 UTC
- Open issues: 89 total (5 P1, 83 P2) + 1 INFO
- Last clean run: never

## Standing context
- Step A.2.4 Performance migration ran 2026-08-09 and is complete — do NOT run it again. Performance findings use stable issue text (`LCP above threshold`, `CLS above threshold`, `INP above threshold`, `Performance score below target`) with no metric values. Measured numbers live in `performance-report.md` only.
- The stable-identity scheme continues to hold: this run minted only 1 new Performance ID (P-430) against 89 open findings. If a run ever mints new IDs for most of the inventory again, re-read Step 3.4 before writing findings.md.
- **2026-08-17 fix+redeploy verified working:** diagnosed 5 stale Performance P1s (`/contact`, `/about`, `/faqs`, `/careers`, `/roofs-for-heroes`) sharing a root cause — 12 static-route pages hardcoded raw unoptimized Sanity CDN hero image URLs instead of routing through `urlFor()` (`lib/sanity-image.ts`), serving full JPEG originals instead of CDN-negotiated WebP/AVIF. Fixed in commit `eaae14f`, merged via PR #70 to `main`, confirmed live in production via curl before this rerun. Result: 20 Performance findings resolved, open P1 count dropped 14 → 5. `/contact` LCP 9354ms→2176ms, `/about` 4358ms→1876ms (now fully clean), `/careers` 4201ms→2177ms (now fully clean), `/roofs-for-heroes` 11191ms→3376ms, `/faqs` 4201ms→2851ms. None of the remaining 5 open P1s (`/service-areas`, `/services/roof-repair`, `/services/skylight-installation`, `/blog/when-to-get-roof-rejuvenation-frederick-md`, `/blog/how-marylands-summer-heat-affects-roofs-in-frederick-md`) were touched by this fix — they need separate investigation. `/services/chimney-maintenance` and `/services/roof-installation` (P1 last run) also weren't part of the hardcoded-URL group but roof-installation resolved anyway (4351ms→2401ms) and chimney-maintenance improved to P2 (5542ms→3635ms) — worth checking if those pages share Sanity-driven `servicesImage` optimization already, or improved from general variance.
- **PageSpeed lab-run flakiness observed:** first sweep pass produced 3 outlier readings (`/contact` 13085ms LCP, `/testimonials` 8055ms, `/service-areas` 5647ms) whose own `lcp-breakdown-insight` component durations didn't sum anywhere near the reported total (e.g. /contact's breakdown summed to ~2.5s vs a reported 13s) — a strong tell for a bad single lab run rather than a real value. Retested each 1-2x for confirmation before writing findings. If a future run sees a metric wildly inconsistent with its own breakdown-insight detail, retest before trusting it rather than filing a P1 off one flaky sample.
- Health and Technical SEO/GSC remain structurally clean: all 49 sitemap URLs return 200 with 0 redirect hops, no dead internal links, no orphans, no missing/duplicate meta tags, no canonical or JSON-LD defects, all 49 `<lastmod>` present. The only non-Performance findings are the same 11 Sanity image-field gaps (10 `servicesImage`, 1 `featuredImage`) carried since 2026-06-24/2026-08-09, plus 1 INFO noindex flag on `/blog/how-much-do-new-gutters-cost-maryland`.
- Sitemap lives at `https://shumakerroofing.com/sitemap.xml` (non-www). The www host 308-redirects to non-www, so fetch with `curl -L`; the `<loc>` values are all non-www with no trailing slash.
- PageSpeed Insights sweep of 49 URLs takes ~8-9 minutes sequentially (1s delay/call + ~7-10s API latency each) — run as a background Bash job with a Monitor watching for a `DONE` sentinel line rather than a single foreground call (10-minute tool timeout can cut it off partway).
- Machine clock is Asia/Manila (UTC+8); local date runs a day ahead of UTC. Report timestamps use UTC.
