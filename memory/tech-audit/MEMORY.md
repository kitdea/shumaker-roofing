# Tech Audit Memory

- Last run: 2026-09-09 21:34 UTC
- Open issues: 74 total (12 P1, 61 P2) + 1 INFO
- Last clean run: never

## Standing context
- Step A.2.4 Performance migration ran 2026-08-09 and is complete — do NOT run it again. Performance findings use stable issue text (`LCP above threshold`, `CLS above threshold`, `INP above threshold`, `Performance score below target`) with no metric values. Measured numbers live in `performance-report.md` only.
- **2026-09-09: PSI lab flakiness was the worst on record — 4 full sweeps and 8+ breakdown-insight spot-checks were needed, and it never fully resolved.** Sweep 1 (49/52 failing) matched the known infra-congestion signature and was discarded. Sweep 2 was corrupted by a *different* failure mode: the sweep-1 background process (started via `nohup ... &`, not the harness's tracked background execution) did not actually die when its Monitor timed out — it kept running detached and collided with sweep 2's writes to the same output file, producing 8 duplicate URL rows and several HTTP 500s. **Lesson: always launch long-running sweeps via the tool's `run_in_background: true` (tracked, killable) rather than shell-level `nohup &` (untracked, can outlive the session's awareness of it).** Sweeps 3 and 4 were both clean single-process runs with no duplicates, but still disagreed sharply per-URL (e.g. `/` read 9621ms in sweep 3, 2626ms in sweep 4) — direct `lcp-breakdown-insight` spot-checks on the outliers, across all 4 sweeps, consistently showed the breakdown summing to only 200-2500ms regardless of how high the reported LCP was (up to 17951ms for `/contact`), confirming the readings — not the pages — were unreliable. **Final approach used: per-URL, took the lower of sweep-3/sweep-4 readings as this run's dataset.** 7 URLs never produced a low/clean reading in either full sweep (`/services`, `/blog`, `/services/gutter-installation`, `/services/roof-replacement`, `/blog/how-marylands-summer-heat-affects-roofs-in-frederick-md`, `/blog/author/tyler-schisler`, `/contact`) — their Performance findings (6 P1s: P-432, P-446, P-447, P-456, P-462, P-464, P-466; plus related P2 score findings) are flagged in `findings.md` as "⚠ PSI lab flakiness — inconclusive" rather than asserted as confirmed regressions, and should be manually re-verified via the PageSpeed Insights web UI before acting on them. `/contact` is the most concerning of these — it only produced one non-error reading all run (17951ms) and has a real history of past severe LCP issues (see 2026-08-17/2026-08-28 notes), so a genuine regression there can't be ruled out with today's data alone.
- **Actionable takeaway, refined further:** the existing "spot-check 2-3 outliers, discard-and-rerun-once" protocol assumes one bad sweep followed by one clean one. That assumption failed today — assume flakiness may take 3-4+ sweeps to resolve (or may never fully resolve in one session), budget PSI time accordingly (~10-12 min per sweep at 1s or 4s delay), and be willing to report a composite/best-of-N dataset with explicit inconclusive-flags rather than blocking indefinitely on a single "clean" sweep.
- This run resolved: H-017/H-021 (`servicesImage` now present for `commercial-flat-and-low-slope-roofing-restoration` and `roof-repair`), H-049 (`/blog/replacing-gutters-with-new-roof-frederick-md` now in sitemap.xml). New: H-052 (`/services/siding` — a new service page, missing `servicesImage`), S-081/S-082 (`/blog/replacing-gutters-with-new-roof-frederick-md` and `/service-areas/hagerstown-md` both entirely missing `<meta name="description">` — genuine P1s, verified directly against raw HTML, not a parser false-negative), S-083/S-084/S-085 (related missing og:description/og:image on the same two URLs).
- Sitemap grew from 50→52 URLs since 2026-08-28: two new pages, `/services/siding` and `/services/metal-roofing` was already present. Health and Technical SEO otherwise clean: 52/52 URLs return 200 with 0 redirect hops, no dead internal links, no orphans, no duplicate titles, no canonical defects, all JSON-LD blocks valid with required fields present (LocalBusiness/RoofingContractor, Article, FAQPage, ProfilePage all checked via a Node-based parser, not regex — @graph-nested types handled correctly this run after an earlier top-level-only jq check produced false negatives).
- Sitemap lives at `https://shumakerroofing.com/sitemap.xml` (non-www). The www host 308-redirects to non-www — fetch with `curl -L`; `<loc>` values are all non-www with no trailing slash.
- GSC readiness fully clean: verification tag present, GTM-MHJVRR9 detected, sitemap reachable and matched in robots.txt, canonical domain enforced via 1-hop 308 redirect.
- Machine clock is Asia/Manila (UTC+8); local date runs a day ahead of UTC. Report timestamps use UTC.
- **Tooling note:** this session's Bash environment has `jq` and `node` available but no `python3` — use those for JSON/HTML parsing instead. Windows path translation: bash `/tmp/foo` corresponds to `C:\Users\<user>\AppData\Local\Temp\foo` for the Read/Write tools (use `cygpath -w` to convert). Node scripts run with `cwd` on the Windows drive (e.g. `D:\...`) will resolve a bash-style `/tmp/...` argument as `D:\tmp\...` — always pass the full `C:/Users/.../AppData/Local/Temp/...` path to `fs.readFileSync` from a Node one-liner invoked via Bash.
