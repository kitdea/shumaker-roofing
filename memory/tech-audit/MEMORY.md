# Tech Audit Memory

- Last run: 2026-09-14 09:00 UTC
- Open issues: 88 total (7 P1, 81 P2)
- Last clean run: never

## Standing context
- Step A.2.4 Performance migration ran 2026-08-09 and is complete — do NOT run it again. Performance findings use stable issue text (`LCP above threshold`, `CLS above threshold`, `INP above threshold`, `Performance score below target`) with no metric values. Measured numbers live in `performance-report.md` only.
- **Recurring gotcha — Node `/tmp/...` and `/c/...` path resolution on Windows:** a Bash `curl -o /tmp/x.json` or `curl -o /c/Users/.../x.json` writes correctly (bash translates these to the real Windows path), but a `node -e` one-liner invoked from that same Bash session resolves a literal `/tmp/...` or `/c/...` argument relative to the project's `D:\` drive and throws `ENOENT` — silently, since the bash loop had no `set -e`. This broke the 2026-09-11 run's first PSI sweep in the past. **Fix used again successfully this run (2026-09-14):** always pass the full `C:/Users/franc/AppData/Local/Temp/...` path to any `fs.readFileSync`/`fs.writeFileSync`/`fs.appendFileSync` call inside a `node -e` script or a `.js` file run via `node <path>` — never a bash-style `/tmp/...` or `/c/...` path — even though the same path works fine for `curl -o`, `cat`, `grep`, etc. within pure-bash commands. Confirmed again this run: curl-written JSON files opened fine once the node read used the `C:/...` form.
- **2026-09-14: PSI lab flakiness recurred, 4th run in a row.** Sweep 1 showed 51/52 URLs failing (bimodal LCP: clean readings ~1800-3200ms vs congested readings ~6600-16000ms for the same URL across sweeps) — matched the known infra-congestion signature, so ran the full 3-sweep protocol. Sweep1↔Sweep2 pass/fail disagreement was low this time (1/52) but per-URL LCP values still swung wildly across all 3 sweeps, so all 3 were combined as usual. Used lowest-LCP-of-3 as the working dataset. 6 URLs were consistently >4000ms across all 3 sweeps with zero low readings — treated as confirmed regressions: `/privacy-policy`, `/terms-and-conditions`, `/services/siding`, `/blog/what-to-expect-roof-rejuvenation-frederick-md`, `/blog/when-to-get-roof-rejuvenation-frederick-md`, `/blog/best-roofing-contractors-in-frederick-md`.
- **`/contact` update (2026-09-14):** flagged in 3 prior runs (2026-08-28 through 2026-09-11) for severe LCP (11.7s-19s). This run's 3 sweeps were 3151/3151/8640ms — the lowest reading is far better than prior runs and only 1 of 3 sweeps hit the congested band, so it did NOT meet the "confirmed" bar this run (no low reading required; `/contact` had two). Downgraded P1→P2 in findings.md (P-466, P-323 both now P2). This reads as a real improvement, but given the 3-run history of severe regression, it's worth a manual out-of-PSI check before fully trusting it's fixed — flagging for next run to watch whether it recurs at the severe band.
- **Actionable takeaway carried forward:** budget for 3+ PSI sweeps per run when the first sweep shows >80% of URLs failing (the discard-signature). A 3rd sweep, used to find per-URL cross-sweep agreement (not just "did it improve"), is what separates genuine regressions from lab noise — single-sweep or even 2-sweep-disagreement data should not be reported as confirmed. Note 2026-09-14: low sweep1↔2 *pass/fail* disagreement does NOT mean the LCP values are stable — always check the raw per-URL LCP spread across all 3 sweeps, not just whether the boolean pass/fail flipped, before deciding a URL doesn't need the 3rd sweep treatment.
- This run (2026-09-14): Health and Technical SEO both essentially unchanged/clean — same 9 Sanity field-integrity P2s (7 services missing `servicesImage`, 2 blog posts missing `featuredImage`) as the last several runs, same 1 open og:image gap (`/blog/replacing-gutters-with-new-roof-frederick-md`, S-085). **New this run:** `/projects` JSON-LD carries a `LocalBusiness` object nested inside `CollectionPage.provider` with `name`/`address` but no `telephone` — flagged P1 as S-086 (new finding, no prior open finding at this URL/field). Checked `memory/seo/audit-findings-log.md` for an existing `metadata:...` finding to reference instead — none found, so this is genuinely new, not a dup.
- Sitemap unchanged at 52 URLs. Health and Technical SEO otherwise clean: 52/52 URLs return 200 with 0 redirect hops, no dead internal links, no orphans, no duplicate titles, no canonical defects. Noindex flags: none detected this run.
- Sitemap lives at `https://shumakerroofing.com/sitemap.xml` (non-www). The www host redirects to non-www (1 hop) — fetch with `curl -L`; `<loc>` values are all non-www with no trailing slash.
- GSC readiness fully clean again: verification tag present (`oTVHtZ3S***`), GTM detected, sitemap reachable and matched in robots.txt, canonical domain enforced via 1-hop redirect. No G- findings have ever been needed (GSC module has been clean every run to date).
- Machine clock may run ahead of UTC — always compute report timestamps in UTC explicitly (`date -u`).
- **Tooling note:** this session's Bash environment has `jq` and `node` available but no `python3` — use those for JSON/HTML parsing instead. See the Node path gotcha entry above — it applies specifically to `node -e`/`node <script>.js` invocations, not to Bash itself (curl/cat/grep handle `/tmp` and `/c/...` bash-style paths fine).
- **Runtime note (2026-09-14):** each individual PageSpeed Insights API call took roughly 15-30s in this environment (real API latency, not a bug) — a single 52-URL sweep takes ~15-20 minutes. Budget accordingly when planning a 3-sweep run (45-60+ minutes total for Module 3 alone); do not mistake slow-but-steady progress for a stuck/hung process.
