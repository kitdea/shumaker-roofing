# Technical SEO Report — 2026-08-17 00:09 UTC

| Check | URL | Finding | Severity |
|-------|-----|---------|----------|
| Noindex flag | /blog/how-much-do-new-gutters-cost-maryland | noindex detected — logged only, no action taken | INFO |

All other checks clean across all 49 sitemap URLs:
- robots.txt returns 200, contains `Sitemap: https://shumakerroofing.com/sitemap.xml`, no critical paths disallowed.
- All 49 sitemap `<url>` entries have `<lastmod>` present.
- All 49 pages have non-empty `<title>` and `<meta name="description">`.
- All 49 pages have `og:title`, `og:description`, `og:image` present.
- All 49 pages have a canonical `<link>` tag matching the expected URL.
- All JSON-LD blocks present validate required fields for their `@type` (LocalBusiness, Service, Article/BlogPosting, FAQPage where applicable). No page is missing a JSON-LD block entirely.
- No duplicate `<title>` values across pages.
- No orphaned pages (cross-referenced against Module 1 internal-link inventory).

**Severity key:** P1 = critical · P2 = warning · INFO = informational only, no action needed
