# Technical SEO Report — 2026-08-28 11:15 UTC

| Check | URL | Finding | Severity |
|-------|-----|---------|----------|
| Noindex flag | /blog/how-much-do-new-gutters-cost-maryland | noindex detected — logged only, no action taken | INFO |

**Severity key:** P1 = critical · P2 = warning · INFO = informational only, no action needed

All other checks clean this run: robots.txt returns 200 with sitemap directive and no critical-path Disallow rules; all 50 sitemap URLs return 200 with `<lastmod>` present; title/meta description/og:title/og:description/og:image present on all 50 pages; canonical tags present and matching on all pages; JSON-LD present and valid on all pages (LocalBusiness on homepage with address+telephone, Service on all service pages with name+provider, Article on all blog posts with headline+author+datePublished, FAQPage with non-empty mainEntity where present); no duplicate titles; no orphaned sitemap pages (0 inbound internal links).
