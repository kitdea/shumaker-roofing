# Technical SEO Report — 2026-08-17 06:35 UTC

| Check | URL | Finding | Severity |
|-------|-----|---------|----------|
| Noindex | /blog/how-much-do-new-gutters-cost-maryland | noindex detected — logged only, no action taken | INFO |

**Severity key:** P1 = critical · P2 = warning · INFO = informational only, no action needed

All other checks clean across all 49 sitemap URLs: robots.txt returns 200 with a `Sitemap:` directive and no critical-path `Disallow` rules; all 49 `<lastmod>` entries present; title/meta description/og:title/og:description/og:image present and non-empty on every page; canonical tags present and matching on every page; JSON-LD present with required fields for LocalBusiness/Service/Article/FAQPage types where applicable; no duplicate titles; zero dead internal links (all internal hrefs resolve to sitemap-inventoried paths or are static assets); zero orphaned pages (every sitemap URL has at least one inbound internal link).
